
import ReactNative from 'react-native';

const {StyleSheet} = ReactNative;

export default StyleSheet.create({
    circle: {
        position: 'absolute',
        zIndex: 999,
        elevation: 10
    }
});
